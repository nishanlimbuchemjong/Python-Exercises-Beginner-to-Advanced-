# Use the sum() function to calculate the sum of numbers in the list [1, 2, 3, 4, 5].

number_list = [1, 2, 3, 4, 5]

# calculating the sum of a given list using sum() built-in function
print(f"Sum of the list {number_list} is {sum(number_list)}")
